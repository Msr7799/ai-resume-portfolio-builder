# تقرير مراجعة مشروع AI Resume & Portfolio Builder

## ملخص سريع
راجعت بنية المشروع والملفات الأساسية الخاصة بالواجهة، الثيم، القوالب، الذكاء الاصطناعي، PDF، المصادقة، التخزين، وملفات البيئة. المشروع متقدم كـ MVP: يوجد Dashboard، Resume Builder، Canva Template Preview، تصدير PDF عبر `@react-pdf/renderer`، Router للذكاء الاصطناعي، MongoDB/Cloudinary APIs، ونظام لغة عربي/إنجليزي.

## الإصلاحات التي تم تنفيذها

### 1. إصلاح مشكلة الثيم اللي كان يمشي على system فقط
سبب المشكلة كان أن Tailwind CSS v4 يستخدم `dark:` حسب إعداد variant. الكود كان يضيف كلاس `dark` على عنصر `html`، لكن Tailwind لم يكن مضبوطًا لكي يعتمد على `.dark`، لذلك كان يتبع ثيم النظام في كثير من الكلاسات.

تمت إضافة هذا السطر في `src/app/globals.css`:

```css
@custom-variant dark (&:where(.dark, .dark *));
```

النتيجة: اختيار Light أو Dark الآن يجب أن يغيّر واجهة الموقع نفسها، وليس فقط عندما يتغير ثيم النظام.

### 2. تحويل زر الثيم من ثلاثة أزرار إلى زر واحد
تم تعديل `src/components/layout/theme-toggle.tsx` ليصبح زرًا واحدًا فقط، ويتنقل بهذا التسلسل:

```text
System → Light → Dark → System
```

الأيقونة تتغير حسب الوضع:
- System: أيقونة الشاشة
- Light: أيقونة الشمس
- Dark: أيقونة القمر

### 3. إصلاح ثغرة صلاحيات في التسجيل وتعديل البروفايل
كان بإمكان الطلب المرسل إلى API أن يحتوي على `role: "admin"` أثناء التسجيل أو تعديل البروفايل. هذا خطر لأن المستخدم العادي لا يجب أن يحدد دوره من جهة العميل.

تم التعديل:
- في `src/app/api/auth/sign-up/route.ts`: أي تسجيل عام يأخذ role = `user` فقط.
- في `src/app/api/profile/route.ts`: تعديل البروفايل لا يسمح بتغيير role.

### 4. تنظيف `.env.example`
تم حذف تكرار `MONGODB_URI=` الموجود في آخر الملف.

### 5. دعم مسار القالب القديم والجديد
الملف الموجود في المشروع كان هنا:

```text
public/templates/My-CV.png
```

وأنت سابقًا ذكرت مسار:

```text
public/templets/My-CV.png
```

أضفت نسخة متوافقة في `public/templets/My-CV.png` حتى لا ينكسر المشروع لو استعملت المسار القديم.

## ملاحظات على تجربة المستخدم

### نقاط جيدة
- واجهة الموقع منظمة وواضحة.
- وجود اللغة العربية والإنجليزية ممتاز للفكرة.
- Canva Template flow صحيح: المستخدم يختار قالبًا، ثم يملأ حقوله، ثم يعاين، ثم يصدر PDF.
- PDF مبني بطريقة صحيحة نسبيًا: الخلفية صورة، لكن النصوص والروابط والـ QR فوقها كعناصر حقيقية.
- وجود تنبيهات عندما يتجاوز النص عدد الأحرف أو الأسطر يساعد المستخدم.
- فصل AI router عن client-side جيد؛ التوكن لا يظهر في المتصفح.

### ملاحظات تحتاج تطوير لاحقًا
1. بعض الصفحات تستخدم نصوص عربية ثابتة بدل نظام الترجمة `t()`، وهذا يجعل تجربة اللغة غير موحدة.
2. React-PDF مع العربية قد يحتاج خط عربي مخصص وتجربة RTL أعمق، لأن `Helvetica` لا يعطي أفضل نتيجة للعربي دائمًا.
3. معرض القوالب يحتاج لاحقًا أداة تعديل أماكن الحقول بصريًا بدل تعديل x/y يدويًا.
4. زر AI يرجع fallback صامت إذا فشل الطلب؛ الأفضل عرض رسالة صغيرة تقول إن الرد من mock fallback.
5. رفع الصور الشخصية داخل التمبلت يستخدم base64 في localStorage؛ هذا مناسب للـ MVP، لكنه ليس مناسبًا على المدى الطويل. الأفضل رفع الصور إلى Cloudinary وتخزين URL.
6. Auth جيد كبداية، لكن لا تعتمد على fallback secret في الإنتاج. لازم تضبط `AUTH_SECRET` قوي.
7. يجب إضافة rate limiting على `/api/ai/improve` حتى لا يتم استهلاك Hugging Face token بسرعة.
8. صفحة إعدادات/بروفايل تحتاج توضيح هل البيانات محلية أو محفوظة في MongoDB.

## ملفات تم تعديلها

```text
src/app/globals.css
src/components/layout/theme-toggle.tsx
src/app/api/auth/sign-up/route.ts
src/app/api/profile/route.ts
.env.example
public/templets/My-CV.png
REVIEW_REPORT_AR.md
```

## لم أتمكن من تشغيل build داخل بيئة الفحص
حاولت تشغيل pnpm، لكن بيئة الفحص لا تحتوي pnpm جاهزًا، وCorepack لم يستطع تحميله بسبب عدم توفر اتصال بالـ npm registry داخل البيئة. لذلك لم أستطع تشغيل:

```bash
pnpm install
pnpm lint
pnpm build
```

يجب تشغيلها عندك بعد فك الضغط:

```bash
pnpm install
pnpm lint
pnpm build
pnpm dev
```

## اختبار إصلاح الثيم
1. شغل المشروع:

```bash
pnpm dev
```

2. افتح الصفحة الرئيسية.
3. اضغط زر الثيم مرة بعد مرة.
4. تأكد أن الوضع يتغير بهذا الشكل:

```text
النظام → فاتح → داكن → النظام
```

5. غيّر نظام الجهاز إلى Dark ثم اختبر أن اختيار Light داخل الموقع يجبر الموقع على النهاري.
6. غيّر نظام الجهاز إلى Light ثم اختبر أن اختيار Dark داخل الموقع يجبر الموقع على الليلي.
